load("@rules_haskell//haskell:defs.bzl", "haskell_binary")

haskell_binary(
    name = "day02",
    srcs = glob(["Main.hs"]),
    deps = [
        "@stackage//:base",
        "@stackage//:parsec",
    ],
)
