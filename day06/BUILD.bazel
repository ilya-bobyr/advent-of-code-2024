load("@rules_haskell//haskell:defs.bzl", "haskell_binary")

haskell_binary(
    name = "day06",
    srcs = glob(["Main.hs"]),
    deps = [
        "@stackage//:base",
        "@stackage//:array",
    ],
)
